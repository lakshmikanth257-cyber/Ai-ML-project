# RabbitMQ configuration for integration tests
#
# Set memory threshold to 80% to avoid false alarms in test environment
vm_memory_high_watermark.relative = 0.8

# Reduce heartbeat timeout for faster failure detection
heartbeat = 60

# Disable disk free space alarm for test environment
disk_free_limit.absolute = 50MB
